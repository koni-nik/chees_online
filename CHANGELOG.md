# Changelog - История изменений

## Версия 2.8 - Система аккаунтов и улучшения ⭐

### Новая функциональность

1. **Система аккаунтов**
   - Регистрация и вход с email/password
   - JWT-аутентификация для HTTP и WebSocket endpoints
   - Хеширование паролей с использованием bcrypt
   - Верификация email для новых аккаунтов
   - Восстановление пароля через email
   - Профили пользователей (username, email, created_at, last_login)

2. **База данных**
   - Новая таблица `users` для хранения аккаунтов
   - Таблицы `user_sessions`, `email_verification_tokens`, `password_reset_tokens`
   - Связь между `users` и `players` через `user_id`
   - Миграция существующих игроков в систему аккаунтов

3. **Безопасность**
   - Rate limiting для auth endpoints (slowapi)
   - JWT токены (access + refresh)
   - Защита WebSocket соединений через JWT
   - Middleware для аутентификации

4. **Производительность**
   - Кэширование рейтингов в памяти (cachetools.TTLCache)
   - Оптимизированный matchmaking сервис

5. **Рефакторинг backend**
   - Модульная структура: `routes/`, `services/`, `middleware/`, `handlers/`
   - Вынесена логика matchmaking в `services/matchmaking_service.py`
   - Обработчики WebSocket в `handlers/websocket_handler.py`
   - Auth endpoints в `routes/auth.py`

6. **Frontend v2.8**
   - Новый интерфейс аутентификации (login/register)
   - Интеграция с JWT токенами
   - Управление сессиями
   - Формы верификации email и восстановления пароля

### Технические детали

- **Новые зависимости**: `bcrypt`, `python-jose[cryptography]`, `aiosmtplib`, `slowapi`, `cachetools`
- **Новые модули**: `backend/routes/auth.py`, `backend/services/email_service.py`, `backend/services/matchmaking_service.py`, `backend/middleware/auth_middleware.py`, `backend/middleware/rate_limit.py`, `backend/handlers/websocket_handler.py`
- **Миграция**: `backend/migrations/migrate_to_v2_8.py` для переноса существующих игроков

### Обратная совместимость

- Старые версии frontend (v2.5, v2.6, v2.7) продолжают работать без аутентификации
- Существующие `player_id` мигрируются автоматически в guest аккаунты

---

# Changelog - Исправления кода

## Критичные исправления безопасности ✅

1. **CORS настройки**
   - Добавлена конфигурация через переменные окружения
   - По умолчанию разрешены только локальные домены
   - Добавлен флаг `ALLOW_ANY_ORIGIN` для разработки

2. **Валидация входных данных**
   - Добавлена валидация `player_id` и `room_id` через регулярные выражения
   - Защита от инъекций и некорректных данных
   - Валидация в path параметрах WebSocket endpoints

3. **Обработка ошибок**
   - Улучшена обработка конкретных типов ошибок (ValidationError, KeyError, ValueError, TypeError)
   - Более информативные сообщения об ошибках

## Исправленные баги ✅

1. **Сохранение игры при отключении**
   - Исправлена логика определения `white_id` и `black_id`
   - Правильная обработка отключившегося игрока

2. **Race condition в matchmaking**
   - Добавлена защита от одновременного удаления игроков из очереди
   - Обработка исключений при удалении

3. **Утечки памяти**
   - Добавлена автоматическая очистка пустых комнат
   - Периодическая очистка неактивных комнат (каждые 5 минут)

## Рефакторинг ✅

1. **Менеджеры для управления состоянием**
   - `RoomManager` - управление игровыми комнатами
   - `TournamentRoomManager` - управление турнирными комнатами
   - `ConnectionManager` - управление WebSocket соединениями
   - Все глобальные переменные вынесены в классы

2. **Логика undo**
   - Добавлен метод `undo_move()` в `ChessGame`
   - Правильный откат хода вместо пересоздания игры
   - Сохранение состояния игры

3. **Конфигурация**
   - Создан файл `config.py` для всех настроек
   - Поддержка переменных окружения
   - Расширяемость для новых версий фронтенда и тем

## Улучшения качества кода ✅

1. **Удалены неиспользуемые импорты**
   - Удален `heapq`
   - Удален дублирующий `import time`

2. **Использование констант**
   - Заменены магические числа на константы из `config.py`
   - Использование `DEFAULT_TIME_CONTROL` вместо `600`

3. **Реализован TODO**
   - Реализована проверка шаха в `analysis.py`
   - Использование `ChessRules.is_in_check()`

## Расширяемость ✅

1. **Конфигурация версий**
   - Список доступных версий в `config.FRONTEND_VERSIONS`
   - Легко добавить новые версии

2. **Конфигурация тем**
   - Подготовлен список доступных тем в `config.AVAILABLE_THEMES`
   - Готовность к будущей реализации

## Производительность (частично) ⚠️

1. **Оптимизация matchmaking**
   - Защита от race conditions
   - Улучшена обработка ошибок

2. **Очистка ресурсов**
   - Автоматическая очистка пустых комнат
   - Периодическая очистка неактивных комнат

## Примечания

- Все изменения обратно совместимы
- Функционал и визуальное отображение не изменены
- Код готов к дальнейшей доработке
- Добавлена возможность легко расширять функционал

## Следующие шаги (опционально)

1. Добавить connection pooling для БД
2. Добавить кэширование рейтингов
3. Добавить rate limiting middleware
4. Расширить тестовое покрытие

